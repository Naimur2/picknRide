import { TouchableOpacity } from "react-native";
import { Box } from "native-base";

export type TTouchableOpactity = React.ComponentProps<typeof TouchableOpacity>;
export type TBoxProps = React.ComponentProps<typeof Box>;
